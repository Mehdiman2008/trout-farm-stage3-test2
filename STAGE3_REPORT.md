# گزارش مرحله ۳ — موتور تصمیم خرید و فروش

## باگ عملیاتی ثبت رویداد — رفع شد

**ریشه مشکل** در `showTab` بود:

```js
if (t === "txns" && !TXNS.length) { buildTxnForm(); loadTxns(); }
```

`loadTxns()` هنگام راه‌اندازی اجرا می‌شد، پس `TXNS.length` همیشه بزرگ‌تر از
صفر بود و **`buildTxnForm()` هرگز صدا زده نمی‌شد**. فرم خالی می‌ماند،
`$("#f_type")` برابر `null` بود و کلیک روی دکمه با TypeError بی‌صدا شکست
می‌خورد — بدون هیچ پیامی به کاربر.

### اصلاحات

| مورد | تغییر |
|---|---|
| ساخت فرم | حالا بر مبنای خالی بودن **خودِ فرم** است، نه فهرست تراکنش‌ها؛ و یک بار هم هنگام راه‌اندازی ساخته می‌شود |
| اعتبارسنجی سمت سرور | `_validate_txn()` **پیش از هر نوشتنی** اجرا می‌شود، پس رویداد ناقص هرگز ذخیره نمی‌شود |
| اعتبارسنجی سمت کاربر | فیلدهای الزامی با `*` علامت‌گذاری و پیش از ارسال بررسی می‌شوند |
| dropdown واقعی | cohort، استخر، نوع خوراک و دسته هزینه همه از داده واقعی می‌آیند؛ متن آزاد فقط در یادداشت |
| نوع «نگهداری و تعمیرات» | به‌عنوان alias روی `operating_cost` با دسته از پیش انتخاب‌شده |
| پیام موفقیت | `Transaction saved successfully` + شماره رویداد، تاریخ، مقدار، واحد و زمان ثبت |
| پیام خطا | صریح و فارسی، با تأکید بر اینکه هیچ داده ناقصی ذخیره نشده |
| بازمحاسبه کامل | فهرست، Live Farm State، cohortها، استخرها، خوراک، دفتر نقدی، KPI و برنامه |
| Enter | در هر فیلد رویداد را ثبت می‌کند |

### تست‌های end-to-end (۳۴ تست در `test_txn_entry.py`)

* خرید تخم → cohort ساخته می‌شود، مبلغ و واحد درست، در دفتر نقدی دیده می‌شود
* خرید خوراک → موجودی انبار و میانگین موزون قیمت به‌روز می‌شود
* تلفات → موجودی زنده cohort و کل مزرعه کم می‌شود
* فروش → موجودی cohort کم و دفتر نقدی با تقسیم ۵۰/۵۰ به‌روز می‌شود
* هزینه نگهداری → در دسته درست دفتر نقدی ظاهر می‌شود
* ۱۱ حالت ورودی نامعتبر → همه رد می‌شوند و **تعداد رکوردها تغییر نمی‌کند**
* ویرایش پس از ثبت → زنجیره audit trail کامل حفظ می‌شود

---

## مرحله ۳ — موتور تصمیم

### فایل جدید: `core/offers.py`

سه ابزار، همگی تغذیه‌شده از **Live Farm State همان لحظه** — نه از baseline ثابت.

### الف. ارزیابی آفر تخم

ارزش یک lot از راه **حاشیه‌ای** سنجیده می‌شود: بهترین برنامه ممکن *با* آن
در برابر بهترین برنامه ممکن *بدون* آن. اگر lot را با قیمت صفر وارد کنیم،
اختلاف دو برنامه همان کل ارزشی است که این تخم‌ها می‌سازند:

```
حداکثر قیمت توجیه‌پذیر = ارزش افزوده با قیمت صفر ÷ تعداد تخم
```

این عدد در تست تأیید شده: در همان قیمت، سود افزوده تقریباً صفر می‌شود.

خروجی: BUY / PARTIAL_BUY / REJECT · حداکثر قیمت · تعداد پیشنهادی ·
اثر بر سود (اسمی و نامساعد) · بقای مورد انتظار · وزن برداشت بهینه ·
اثر بر ظرفیت در ۳۰/۶۰/۹۰/۱۴۰ روز · اثر بر نیاز نقدی · اطمینان.

**نمونه واقعی:** آفر ۱۶۰٬۰۰۰ تخم با قیمت ۶٬۷۰۰ تومان →
**REJECT**، چون حداکثر قیمت توجیه‌پذیر ۶٬۱۵۳ تومان است. یعنی ۵۴۷ تومان بر
هر تخم گران است و خرید سود کل را ۲۷٫۷ میلیون تومان کم می‌کند.

### ب. ارزیابی آفر فروش — سه قیمت

| قیمت | معنی |
|---|---|
| **کف حسابداری** | بهای تمام‌شده تاریخی هر ماهی. زیر آن، زیان دفتری. |
| **کف تصمیم اقتصادی** | قیمتی که فروش امروز با نگه‌داشتن برابر می‌شود. **مهم‌ترین عدد.** |
| **قیمت پیشنهادی متقابل** | کف اقتصادی به‌علاوه حاشیه چانه‌زنی. |

کف اقتصادی عمداً **هزینه گذشته را نادیده می‌گیرد** — sunk است. آنچه اهمیت
دارد ارزش آینده ماهی است: `بقا × قیمت وزن هدف − خوراک تا آن وزن`.

شرایط پرداخت هم ارزش‌گذاری می‌شود: ۵۰٪ نقد و ۵۰٪ با ۴۵ روز تأخیر، ارزش
امروزِ قیمت را کم می‌کند.

**نمونه واقعی:** فروش ۵۰٬۰۰۰ قطعه با قیمت ۱۶٬۰۰۰ تومان →
**ACCEPT** · کف حسابداری ۹٬۰۰۰ · کف اقتصادی ۱۵٬۳۱۷ · پیشنهاد متقابل ۱۶٬۵۴۲ ·
۱ استخر و ۴۵۰ میلیون تومان سرمایه آزاد می‌شود · ۱۳٫۲ میلیون بهتر از نگه‌داشتن.

### ج. What-If

سناریوهای ترکیبی بدون دست‌زدن به داده واقعی: خرید تخم، فروش ماهی، تغییر
درصدی قیمت خوراک، تلفات یا قیمت فروش. فرضیات موقتاً عوض و در `finally`
حتماً برگردانده می‌شوند — با تست تأیید شده که هیچ رکوردی و هیچ فرضی
پس از اجرا تغییر نکرده است.

---

## تست‌ها

```
242 passed · 0 failed   (۴ دقیقه و ۴۸ ثانیه)
  test_biology.py        15    test_planning.py       39
  test_integration.py    11    test_stage2_final.py   56
  test_corrections.py    34    test_txn_entry.py      34
  test_attribution.py    23    test_offers.py         30
```

## endpointهای جدید

| مسیر | کار |
|---|---|
| `GET /api/decide/context` | وضعیت لحظه‌ای که همه تصمیم‌ها از آن ساخته می‌شوند |
| `POST /api/decide/egg` | ارزیابی آفر خرید تخم |
| `POST /api/decide/sale` | ارزیابی آفر فروش با سه قیمت مرجع |
| `POST /api/decide/what-if` | شبیه‌سازی سناریو |
| `GET /api/solver` | سلامت واقعی حل‌کننده (نصب بودن + اجرای CBC) |

## STOP

مرحله ۴ (یادگیری تطبیقی و لایه AI) شروع نشده است.
