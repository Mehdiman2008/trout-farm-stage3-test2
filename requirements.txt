# وابستگی‌های اجرای داشبورد
PyYAML>=6.0
openpyxl>=3.1      # خواندن فایل اکسل نرخ دلار

# مرحله ۲ — بهینه‌سازی MILP (حل‌کننده CBC همراه بسته می‌آید)
PuLP>=3.0
# اگر PuLP نصب نباشد، برنامه‌ریز به‌صورت خودکار به حالت heuristic برمی‌گردد.

# اختیاری برای تست:
# pytest>=8.0
